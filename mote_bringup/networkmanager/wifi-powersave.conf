# Disable WiFi power save for headless robot use.
# Broadcom brcmfmac + power save can stall SDIO and hang existing SSH sessions.
# Install: pixi run wifi-powersave
#
# wifi.powersave: 0=default, 1=ignore, 2=disable, 3=enable
[connection-wifi]
wifi.powersave = 2
