{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://mote.clach.dev/schema/v1/command.schema.json",
  "title": "Task Command",
  "description": "A task command issued to a robot, identified by a correlation id.",
  "type": "object",
  "required": ["schema", "id", "command", "issued_at"],
  "properties": {
    "schema": {
      "type": "integer",
      "const": 1,
      "description": "The payload format version."
    },
    "id": {
      "type": "string",
      "description": "Unique correlation id for this command, used to track status updates."
    },
    "command": {
      "type": "string",
      "description": "The task command text."
    },
    "issued_at": {
      "type": "string",
      "description": "RFC 3339 UTC timestamp with millisecond precision when the command was issued."
    },
    "issued_by": {
      "type": "string",
      "description": "Identifier of the entity that issued the command."
    }
  }
}
